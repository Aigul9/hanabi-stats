+output folders:
+misc:
+no variant
+purples
+teammates

+notes:
+portraits
+frequent_words
+notes_count
+notes_rates
+vocabulary_intersection

+rank:
+pmdc
+clean games
+end condition
+preference
+risky players

+scores:
+total

+time:
+hours_wr
+peaks
+times spent
+top degenerates

+variants:
+fav suits
+games per max score
+popular vars

+winrate:
+starting_player
+highest wr
+losing streak
+teams wr
+winning streak

+py:
-calc
-eff
+end_condition
+fav_suits
+notes_portrait
+notes_vocabulary
+notes_frequency
+notes_rate
+purple_looker
+purples
+starting_player
+time_spent
+total_max_scores
+utils
-winnable_seeds

pdoc --html py/purples.py --force
pdoc --html py/ --html-dir docs
TODO: the most popular suits on the website

# def most_talkative(data):
#     talk = {}
#     for k, v in data.items():
#         stats = filter_id_range(u.open_stats(k))
#         talk[k] = round(sum([int(r) for r in v.values()]) / len(stats) * 100)
#     return {k: v for k, v in sorted(talk.items(), key=lambda x: (-x[1]))}